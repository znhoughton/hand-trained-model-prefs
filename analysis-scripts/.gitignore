misc_analyses.ipynb
opt-training.ipynb

# brms compilation caches (regenerable, platform-specific)
brms_cache/
model_cache/

# cmdstanr compilation artefacts inside models/ (regenerable)
models/*.hpp
models/*.o
models/*.d
models/stan_model*/
